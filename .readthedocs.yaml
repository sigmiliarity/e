version: 2

build:
    os: ubuntu-20.04
    tools:
      python: "3.8"
    apt_packages:
      - rsync
    commands: 
      - mkdir --parents _readthedocs/html/
      - rsync -av --exclude='_readthedocs' . _readthedocs/html/
