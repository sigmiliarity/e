version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.10"
  jobs:
    build:
      html:
        - mkdir -p $READTHEDOCS_OUTPUT/html/
        - cp -rt $READTHEDOCS_OUTPUT/html/ [!_]*

# Explicitly tell RTD you aren't using Sphinx or MkDocs
formats: []
